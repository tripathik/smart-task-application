spring:
  datasource:
    url: jdbc:mysql://localhost:3306/krishnaresource?useSSL=false&serverTimezone=UTC
    username: root
    password: Krishna@1977
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # or validate / none / create / create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8081

thread:
  threadPoolSize: 5